RewriteEngine On
RewriteBase /techstore/

# Redirige todas las solicitudes a index.php
# Excepto si el archivo o directorio existe
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?c=$1 [L,QSA]
